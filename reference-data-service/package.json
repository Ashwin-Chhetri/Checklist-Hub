{
  "name": "checklisthub-reference-data-service",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone HTTP service exposing the GBIF backbone and GADM SQLite reference data over HTTPS, for the Vercel-hosted app (which has no persistent disk for multi-GB files) to call.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "express": "^4.21.0"
  }
}
