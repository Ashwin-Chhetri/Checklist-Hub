// Placeholder Supabase database types.
//
// Once the Supabase project exists and the migrations in supabase/migrations
// have been applied, regenerate this file with:
//
//   npx supabase gen types typescript --project-id <project-id> > src/types/database.types.ts
//
// Until then, `Database` is left untyped so the Supabase client compiles.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
